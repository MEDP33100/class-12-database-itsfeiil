<h1>{{title}}</h1>

<ul>
  {{#each posts}}
    <li>
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
      <small>Author ID: {{this.authorId}} | Created At: {{this.createdAt}}</small>
    </li>
  {{/each}}
</ul>
